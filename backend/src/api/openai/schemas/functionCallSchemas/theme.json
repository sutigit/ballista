{
    "name": "update_theme",
    "description": "Updates, modifies or generates new theme and styles for fictional user interface based on the provided text. Should be called when user intents to modify the theme and styles.",
    "strict": true,
    "parameters": {
        "type": "object",
        "properties": {
            "prompt": {
                "type": "string",
                "description": "The prompt to update the theme and styles from"
            }
        },
        "additionalProperties": false,
        "required": [
            "prompt"
        ]
    }
}